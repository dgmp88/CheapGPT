These SVGs are here as .svelte as as it allows easier control of their styling, and dynamic responses to e.g. colour themes.

Putting them in static/images works too, but makes the dynamic bit harder
